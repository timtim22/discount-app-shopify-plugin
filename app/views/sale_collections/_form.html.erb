<%= form_with(model: sale_collection, local: true) do |form| %>
  <% if sale_collection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sale_collection.errors.count, "error") %> prohibited this sale_collection from being saved:</h2>

      <ul>
      <% sale_collection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <%= form.hidden_field :sale_id, value: @sale.id %>

  <div class="field">
    <%= form.label :select_collection %>
    <%= form.select :collection_id, options_from_collection_for_select(@store_collections, "id", "title"), prompt: "Select collection" %>
  </div>
  <br/>
  <div class="actions">
    <%= form.submit "Add collection" %>
  </div>
<% end %>
<br/>